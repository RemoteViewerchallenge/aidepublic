---
title: "Examples - Volcano SDK"
---

# Examples

Ready-to-run examples demonstrating Volcano SDK features. [View all examples on GitHub →](https://github.com/Kong/volcano-sdk/tree/main/examples)

All examples showcase **automatic MCP tool selection** where the LLM intelligently chooses which tools to call based on the prompt.

## Basic Examples

### basic.ts

Simple single-step agent with LLM-only generation.

```bash
npx tsx examples/basic.ts
```

**Demonstrates:** Agent creation, basic `.then()` and `.run()`

[View source on GitHub →](https://github.com/Kong/volcano-sdk/blob/main/examples/basic.ts)

### automatic.ts

Automatic MCP tool selection - let the LLM choose which tools to call based on the prompt.

```bash
npx tsx examples/automatic.ts
```

**Demonstrates:** Automatic tool discovery and selection with `mcps: [mcp1, mcp2]`

[View source on GitHub →](https://github.com/Kong/volcano-sdk/blob/main/examples/automatic.ts)

### providers.ts

Multi-provider workflow using OpenAI, Anthropic, Mistral, and Llama with automatic MCP tool selection.

```bash
npx tsx examples/providers.ts
```

**Demonstrates:** Per-step LLM provider switching, automatic tool selection, context flow between providers

[View source on GitHub →](https://github.com/Kong/volcano-sdk/blob/main/examples/providers.ts)

## Advanced Patterns

### parallel.ts

Execute multiple steps in parallel with automatic MCP tool selection in each parallel branch.

```bash
npx tsx examples/parallel.ts
```

**Demonstrates:** `.parallel()` with array and dictionary modes, automatic tool selection

[View source on GitHub →](https://github.com/Kong/volcano-sdk/blob/main/examples/parallel.ts)

### branching.ts & branching-simple.ts

Conditional routing with `.branch()` and `.switch()` using automatic MCP tool selection.

```bash
npx tsx examples/branching-simple.ts
npx tsx examples/branching.ts
```

**Demonstrates:** If/else branching and switch/case patterns, LLM-driven tool selection

[View source on GitHub →](https://github.com/Kong/volcano-sdk/blob/main/examples/branching.ts)

### loops.ts & loops-simple.ts

Iterative workflows with automatic tool selection in each iteration.

```bash
npx tsx examples/loops-simple.ts
npx tsx examples/loops.ts
```

**Demonstrates:** `.while()`, `.forEach()`, `.retryUntil()` with MCP tools

[View source on GitHub →](https://github.com/Kong/volcano-sdk/blob/main/examples/loops.ts)

### sub-agents.ts & composition.ts

Build reusable sub-agents with automatic MCP tool integration.

```bash
npx tsx examples/sub-agents.ts
npx tsx examples/composition.ts
```

**Demonstrates:** `.runAgent()` for modular agent design with tool selection

[View source on GitHub →](https://github.com/Kong/volcano-sdk/blob/main/examples/sub-agents.ts)

### advanced-patterns.ts

Combine parallel execution, branching, and loops with automatic MCP tool selection throughout.

```bash
npx tsx examples/advanced-patterns.ts
```

**Demonstrates:** Complex workflows mixing all control flow patterns with intelligent tool selection

[View source on GitHub →](https://github.com/Kong/volcano-sdk/blob/main/examples/advanced-patterns.ts)

## Special Features

### streaming.ts

Stream step results in real-time with automatic MCP tool selection.

```bash
npx tsx examples/streaming.ts
```

**Demonstrates:** `.stream()` method for progressive result delivery, tool selection in streaming mode

[View source on GitHub →](https://github.com/Kong/volcano-sdk/blob/main/examples/streaming.ts)

### token-streaming.ts

Stream individual tokens in real-time as the LLM generates them.

```bash
npx tsx examples/token-streaming.ts
```

**Demonstrates:** Per-step `onToken` callback and stream-level token streaming with metadata for SSE endpoints

[View source on GitHub →](https://github.com/Kong/volcano-sdk/blob/main/examples/token-streaming.ts)

### multi-agent-crew.ts

Multiple specialized agents with distinct roles collaborating sequentially.

```bash
npx tsx examples/multi-agent-crew.ts
```

**Demonstrates:** Multi-agent patterns, specialized instructions, sequential workflow coordination

[View source on GitHub →](https://github.com/Kong/volcano-sdk/blob/main/examples/multi-agent-crew.ts)

## Running Examples

### Prerequisites

```bash
# Clone the repository
git clone https://github.com/Kong/volcano-sdk.git
cd volcano-sdk

# Install dependencies
npm install

# Build the SDK
npm run build
```

### Set Environment Variables

Most examples require API keys. Set them in your environment:

```bash
export OPENAI_API_KEY="your-openai-key"
export ANTHROPIC_API_KEY="your-anthropic-key"
export MISTRAL_API_KEY="your-mistral-key"

# For Bedrock examples
export AWS_REGION="us-east-1"
export AWS_ACCESS_KEY_ID="your-key"
export AWS_SECRET_ACCESS_KEY="your-secret"

# For Vertex examples
export GCP_VERTEX_API_KEY="your-google-key"

# For Azure examples
export AZURE_AI_API_KEY="your-azure-key"
export AZURE_AI_ENDPOINT="https://your-resource.openai.azure.com/openai/responses"
```

### Run an Example

```bash
# Run any example with tsx
npx tsx examples/basic.ts
npx tsx examples/parallel.ts
npx tsx examples/streaming.ts
```

**Tip:** Start with `basic.ts` and `automatic.ts` to understand core concepts, then explore advanced patterns.

## More Examples

Find more examples in the [GitHub repository](https://github.com/Kong/volcano-sdk/tree/main/examples):

- All examples include automatic MCP tool selection
- Production-ready code you can copy and adapt
- Comments explaining key concepts
- Environment variable setup instructions

### Contributing Examples

Have a great use case? [Submit a PR](https://github.com/Kong/volcano-sdk/pulls) with your example to help other developers!
