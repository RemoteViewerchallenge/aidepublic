# Environment Variables Example
# Copy this file to create your environment-specific configurations:
#
# Development:
#   - .env (committed, shared defaults)
#   - .env.local (git-ignored, local overrides)
#
# Production:
#   - .env.production (committed, production defaults)
#   - .env.production.local (git-ignored, sensitive production secrets)
#
# Note: All client-side variables must be prefixed with VITE_ to be exposed

# Google Analytics
# Get your measurement ID from: https://analytics.google.com/
VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX
