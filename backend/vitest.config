// vitest.config.ts or vite.config.ts
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    // Add or update the 'exclude' property
    exclude: [
      'node_modules',
      // You can keep other exclusions you might have
      'dist',
      'volcano',
      'volcano-sdk',
      '.idea',
      '.git',
      '.cache',
    ],
  },
});
